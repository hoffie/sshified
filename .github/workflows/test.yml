name: Run tests

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7

    - name: Set up Go
      uses: actions/setup-go@v7
      with:
        go-version: '1.26'

    - name: Build
      run: make build

    - name: Test
      run: go test -v ./...

    - name: Integration test
      run: |
        cd loadtest
        make prepare
        make run-webserver1 &
        make run-sshified &
        sleep 10
        make run-loadtest
